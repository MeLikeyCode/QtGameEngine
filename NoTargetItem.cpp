#include "Notargetitem.h"

NoTargetItem::NoTargetItem()
{
}

void NoTargetItem::use()
{
    // Get the Entity (Item knows Inventory which knows Entity)
    // Do effect (weather it depends on Entity, its position or not)
}
